# Dev/test dependencies. Installs the runtime set too, so a single
#   pip install -r requirements-dev.txt
# provisions a full local/CI environment. Not installed into the production image.
-r requirements.txt

ruff==0.1.15
pytest==8.0.0
httpx>=0.27.0,<0.28.0
