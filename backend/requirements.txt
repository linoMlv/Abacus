# Runtime dependencies (installed into the production image).
# Dev/test tooling lives in requirements-dev.txt.

# Web framework and ASGI server
fastapi==0.109.0
uvicorn[standard]==0.27.0

# ORM / models and PostgreSQL driver
sqlmodel==0.0.14
psycopg[binary]==3.2.3
alembic>=1.13.0

# CLI (cli.py) and configuration
typer==0.12.5
click==8.1.7
rich==13.7.0
python-dotenv==1.0.1

# Auth, uploads and rate limiting
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
slowapi==0.1.9

# MCP server
mcp>=1.0.0
